Place real-world `.asp` and `.inc` files in this directory for local smoke checks.

Rules:
- keep files anonymized (no secrets, no customer data);
- keep original structure as much as possible;
- keep encoding UTF-8 where practical.

`AspLargeFileSmokeTest.testOptionalRealWorldAspFixtures` will parse every `.asp` and `.inc` file in this folder.

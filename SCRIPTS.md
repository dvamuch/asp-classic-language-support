# Скрипты для тестирования плагина

## 🚀 Быстрый старт

### quick-test.sh - Самый простой способ
```bash
./quick-test.sh
```

**Что делает:**
1. Останавливает старые процессы IDE
2. Собирает плагин
3. Запускает PhpStorm с проектом `/Users/dmitry-zap/PhpstormProjects/asp-test-project`
4. Автоматически открывает файл `CustomerMail2Send.asp`
5. Показывает начальные логи

**Использовать когда:** Нужно быстро протестировать изменения

---

## 🔄 test-cycle.sh - Полный цикл тестирования

```bash
./test-cycle.sh
```

**Что делает:**
- Автоматический цикл: сборка → запуск → мониторинг → перезапуск
- Интерактивное меню с командами:
  - `m` - Мониторинг логов в реальном времени
  - `l` - Проверить логи сейчас
  - `r` - Перезапустить IDE
  - `q` - Выход
- Показывает инструкции что тестировать
- Автоматически открывает тестовый проект

**Использовать когда:** Активная разработка с частыми перезапусками

---

## 📊 check-logs.sh - Анализ логов

```bash
./check-logs.sh
```

**Что делает:**
- Проверяет логи на ошибки ASP
- Показывает последние ERROR записи
- Показывает исключения
- Выводит статистику

**Использовать когда:** IDE уже запущен, нужно проверить логи

---

## 🔧 run-and-monitor.sh - Запуск с мониторингом

```bash
./run-and-monitor.sh
```

**Что делает:**
- Полная автоматизация запуска
- Непрерывный мониторинг логов
- Показывает только ошибки и важные события
- Красиво форматирует вывод

**Использовать когда:** Нужен детальный мониторинг

---

## 📁 Структура файлов проекта

```
asp-classic-language-support/
├── quick-test.sh           ← Самый простой способ
├── test-cycle.sh           ← Полный цикл с перезапуском
├── run-and-monitor.sh      ← Запуск с мониторингом
├── check-logs.sh           ← Проверка логов
│
├── build.gradle.kts        ← Настройки для открытия проекта
│
└── test-project/           ← Тестовые ASP файлы (если нужно)
    ├── index.asp
    └── form.asp
```

---

## 🎯 Тестовый проект

**Путь:** `/Users/dmitry-zap/PhpstormProjects/asp-test-project`
**Главный файл:** `CustomerMail2Send.asp` (774 строки реального ASP кода)

---

## ✅ Что проверять после запуска

1. **Распознавание файла**
   - Файл .asp имеет правильную иконку
   - Внизу справа написано "ASP"

2. **HTML подсветка**
   - `<html>`, `<body>` и другие теги подсвечиваются
   - Атрибуты другого цвета
   - CSS внутри `<style>` имеет CSS подсветку

3. **Автодополнение**
   - Начните печатать `<di` и нажмите Ctrl+Space
   - Должны появиться HTML теги

4. **Навигация**
   - Клик на `<html>` подсвечивает `</html>`
   - Работает для всех тегов

5. **Форматирование**
   - Ctrl+Alt+L (Cmd+Option+L на Mac)
   - Код форматируется с правильными отступами

---

## 🐛 Если что-то не работает

### Проверьте логи:
```bash
./check-logs.sh
```

### Полный лог:
```bash
tail -f build/idea-sandbox/PS-2024.3.1.1/log/idea.log
```

### Перезапуск с чистого листа:
```bash
pkill -9 java
rm -rf build/
./quick-test.sh
```

---

## 💡 Полезные команды

### Только сборка:
```bash
./gradlew clean build
```

### Запуск без сборки:
```bash
./gradlew runIde
```

### Запуск с конкретным проектом:
```bash
./gradlew runIde --args="/path/to/project"
```

### Запуск специальной задачи:
```bash
./gradlew runIdeWithTestProject
```

---

## 📝 Примечания

- Все скрипты автоматически останавливают старые процессы IDE
- Логи сохраняются в `build/idea-sandbox/PS-2024.3.1.1/log/`
- При изменении кода нужен перезапуск IDE
- VBScript блоки `<% %>` пока не поддерживаются (это нормально)

